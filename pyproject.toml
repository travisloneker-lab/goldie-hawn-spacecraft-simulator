[project]
name = "goldie-hawn-spacecraft-simulator"
version = "0.1.0"
description = "A simulated spacecraft flight-software environment."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
test = [
    "pytest",
]

dev = [
    "pre-commit",
    "ruff",
]

[tool.pytest.ini_options]
pythonpath = ["src"]